# Garena 台灣玩家聲音儀表板 — 指令備忘

## 首次設定

### 1. 安裝套件
```bash
cd ~/Desktop/用戶儀表板
pip3 install -r requirements.txt
```

### 2. 填寫 .env
```bash
cp .env.example .env
# 用任何編輯器開啟 .env，填入以下欄位：
# ANTHROPIC_API_KEY  → console.anthropic.com
# NOTION_TOKEN       → notion.so/my-integrations（建立 Integration）
# GMAIL_SENDER       → 你的 Gmail
# GMAIL_APP_PASSWORD → myaccount.google.com/apppasswords（需先開啟兩步驟驗證）
# GMAIL_RECIPIENT    → 收件人信箱
```

### 3. 建立 Notion 資料庫（只需一次）
1. 在 Notion 建立一個新頁面（作為資料庫的父頁面）
2. 將頁面分享給你的 Integration（頁面右上角 `…` → `Connect to` → 選你的 Integration）
3. 複製頁面 URL 中的 ID（32 位英數字串）
4. 執行：
```bash
python3 main.py --init-notion --page-id <PAGE_ID>
```
這會自動建立「玩家回饋分析」和「週報紀錄」兩個資料庫，並將 ID 寫入 .env。

---

## 日常使用

### 測試（不寫 Notion，不寄信）
```bash
python3 main.py --dry-run
```

### 手動執行抓取（抓取 + 分析 + 寫 Notion）
```bash
python3 main.py --scrape
```

### 手動產生週報（彙整 + 寄信）
```bash
python3 main.py --report
```

### 查詢 Notion 資料庫
```bash
python3 main.py --stats                     # 統計資料
python3 main.py --search Bug回報            # 搜尋類別
python3 main.py --search 卡頓               # 搜尋關鍵字
python3 main.py --search 負面 --limit 30   # 搜尋情緒 + 指定筆數
```

---

## launchd 排程設定

### 安裝排程
```bash
cp ~/Desktop/用戶儀表板/launchd/*.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.garena.dashboard.daily.plist
launchctl load ~/Library/LaunchAgents/com.garena.dashboard.weekly.plist
```

### 手動觸發（測試排程是否正常）
```bash
launchctl start com.garena.dashboard.daily
launchctl start com.garena.dashboard.weekly
```

### 查看排程日誌
```bash
tail -f /tmp/garena-dashboard.log
tail -f /tmp/garena-dashboard-error.log
```

### 停用排程
```bash
launchctl unload ~/Library/LaunchAgents/com.garena.dashboard.daily.plist
launchctl unload ~/Library/LaunchAgents/com.garena.dashboard.weekly.plist
```

### 排程時間
| 排程 | 時間 |
|------|------|
| 每日抓取 (`--scrape`) | 每天 07:00 |
| 每週報告 (`--report`) | 每週一 08:00 |

---

## 查看本機日誌
```bash
tail -f ~/Desktop/用戶儀表板/dashboard.log
```

---

## 資料流
```
App Store / Google Play / PTT / Dcard
  → 去重（state.db）
  → 過濾廣告/無意義內容
  → Claude claude-sonnet-4-6 批次分析
  → Notion 玩家回饋分析 DB
  → (每週一) 彙整週報 → Gmail + Notion 週報紀錄 DB
```
